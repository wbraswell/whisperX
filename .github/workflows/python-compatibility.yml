# DISABLED: ZERO-METERED-RESOURCE POLICY
#
# GitHub Actions execution is disabled for this fork because WhisperX currently
# has a zero budget for metered/depletable CI and CI-backed storage.
#
# Do not restore an `on:` trigger or `jobs:` block without explicit advance
# authorization for that exact CI use. Git history preserves the prior Python
# compatibility workflow implementation.
